# Shared Project Instructions

The canonical project instructions are:

@/AGENTS.md

These instructions are mandatory for this workspace.

Always follow the work continuity protocol defined there.

Before continuing previous work, read:

@/.ai/HANDOFF.md

The filesystem, Git state, shared Memory Palace, Context7, and Playwright are shared with OpenCode.

Treat work performed by OpenCode as existing project work that must be inspected and continued rather than recreated.
